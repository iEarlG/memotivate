"use client"

interface DocsNavbarProps {
    isCollapsed: boolean;
    onResetWidth: () => void;
};

export const DocsNavbar = ({
    isCollapsed,
    onResetWidth
}: DocsNavbarProps) => {
    return (
        <div>
            DocsNavbar
        </div>
    )
};